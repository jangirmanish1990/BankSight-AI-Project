# ============================================================
# BankSight AI — Requirements
# Python 3.12+
# Install: pip install -r requirements.txt
# ============================================================

# ── Core AI ─────────────────────────────────────────────────
openai==1.82.0              # OpenAI API — powers SQL and Analysis agents

# ── Streamlit UI ─────────────────────────────────────────────
streamlit==1.57.0           # Chat interface and dashboard UI

# ── Data Processing ──────────────────────────────────────────
pandas==3.0.2               # Data manipulation, CSV loading, cleaning
faker==40.15.0              # Generate mock Indian employee performance data
sqlalchemy==2.0.49          # SQLite ORM — connect agents to banking_mock.db

# ── Reporting ────────────────────────────────────────────────
openpyxl==3.1.5             # Export analysis results to Excel (.xlsx)
reportlab==4.4.10           # Generate PDF churn/spend reports

# ── Visualisation (Streamlit charts) ─────────────────────────
plotly==6.7.0               # Interactive charts inside Streamlit UI

# ── Environment & Config ─────────────────────────────────────
python-dotenv==1.2.2        # Load ANTHROPIC_API_KEY from .env file

# ── Built-in (no install needed) ─────────────────────────────
# sqlite3     — ships with Python, used for banking_mock.db
# os          — file path handling
# random      — mock data generation
# datetime    — date calculations
